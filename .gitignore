# Filigree issue tracker (runtime state; repopulated by the tour / `filigree init`)
.filigree/
.filigree.bak/
.env

# Loomweave index (regenerated by `loomweave analyze`)
.loomweave/

# Plainweave intent store (regenerable; the tour's plainweave_intent leg self-seeds it)
.plainweave/

# Python
__pycache__/
*.pyc
.venv/

# Wardline scan output (regenerated by `wardline scan`)
findings.jsonl
.wardline/

# Legis — local working dir / config (regenerated/local; never commit)
.legis/
legis.yaml

# Legis — local working dir / config (regenerated/local; never commit)
.weft/legis/

# Filigree issue tracker
.weft/

# Migration / local artifacts (the .weft/ store cut-over)
.filigree.conf.imported
.weft-backup-*.tgz

# Subagent-driven-development scratch (progress ledger, task briefs/reports)
.superpowers/

# Filigree server-mode MCP config embeds a literal federation token
.mcp.json
.vscode/

# Rust specimen build artifacts (regenerated by `cargo check`)
specimen-rs/target/
findings-rust.jsonl

# Legis — local working dir / config (regenerated/local; never commit)
/.weft/legis/operator_session.json
/.weft/legis/operator.age
