# wp-reverify-federation

_Generated from `tour/lacunae.toml`. Do not edit by hand._

- **Location:** `specimen/cli.py` → `_add_book`
- **Category:** peer-facts
- **Demonstrates:** `warpline`, `filigree+legis+wardline+warpline`
- **Expected finding:** `warpline` / `wp-reverify-federation`

## Why it's here

NOT A FLAW — a positive warpline capability demo. `warpline_reverify_worklist_get(include_federation=true)` (MCP-only) consults the Weft federation read-only and merges a federation block that ALWAYS names every member — filigree (work), wardline (risk), legis (governance) — each carrying its own weft-reason, plus work/risk/governance on the closed enrichment vocabulary {present,absent,unavailable}. A member that is disabled or unreachable is stated honestly; warpline never implies a clean it did not verify. Governed-present (a verified legis clearance lighting governance=present) is a documented recipe, not asserted here. Advisory/enrich-only, never gates (PDR-0017).

> This is an intentional, permanent demonstration fixture. Removing or "fixing" it fails `make verify`.
