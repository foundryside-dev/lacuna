# Lacuna — the tour

_Generated by `make tour`. Do not edit by hand._

## [PASS] loomweave analyze

indexed the specimen tree into Loomweave's entity / structural-edge graph (live counts ride the non-rendered note for byte-for-byte lockstep)

## [PASS] loomweave structure

dead entities: _add_book, _register, apply_display_option, assert_only_member, audited, broad_handler, contradictory_member, contradictory_member_gap, degenerate_member, eval_report_formula, failopen_member, fetch_cover_image, import_catalog_blob, ingest, invalid_level_member, load_codec_library, load_member_record, load_report_plugin, log_export_request, lookup_member, main, non_rejecting_boundary, notify_member, open_catalog_file, orphaned_helper, parse_catalog_feed, register_member, render_report_template, renewal_count, run_export_command, safe_account_key, shell_archive, swallowed_error, unsafe_account_key; import cycle: cycle_a, cycle_b

## [PASS] loomweave relations

inheritance: Book→Entity, User→Entity, BookRepository→InMemoryRepository, InMemoryRepository→Repository, InMemoryRepository→TimestampMixin, UserRepository→InMemoryRepository; decorators: audited, command

## [PASS] loomweave navigation

execution paths: _add_book (depth 4), _register (depth 4), ingest (depth 4); coupling hotspots: dispatch (10), read_admin_arg (8), read_report_field (6), key (5), add (4); entry points: cli.main; cli subsystem: 11 specimen modules (cli, exception_flow, hub, models, nesting_bomb, pipeline, preview_sinks, repository, service, trust_flow, wardline_sinks)

## [PASS] loomweave findings

analyzer alarms: LMWV-DUPLICATE-LOCATOR, LMWV-PY-TOO-COMPLEX

## [PASS] loomweave rust archaeology

rust index facts: cfg-twin, colon-path-resolved, derives-edge, path-mount

## [PASS] warpline change impact

touching _add_book surfaces downstream service.add_book in blast-radius + reverify worklist (edge-provenanced); change history tracked via churn + timeline — advisory, never gates

## [PASS] warpline attest bundle

handed an attest bundle for a change to _add_book, warpline reports risk_verification=unavailable with an explicit machine reason rather than a silent clean — it echoes a proven-good verdict ONLY when it can mechanically prove the worklist good (impact-complete AND every affected entity attested clean-at-current-body under a newer wardline attestation schema than the installed producer emits), neither of which holds on the installed toolset — advisory, never gates

## [PASS] warpline reverify federation

reverify(include_federation=true) over a change to _add_book names every Weft federation member — filigree work, wardline risk, legis governance — each with its own weft-reason, and reports work/risk/governance on the closed enrichment vocabulary; a member that is absent or unreachable is stated honestly, never implied clean — advisory, never gates

## [PASS] plainweave intent

seeded a covered+uncovered intent corpus over the specimen; cli._add_book and cli.main are justified (SEI->requirement->goal, traced); _register drops from the numerator once its requirement is deprecated; tour.__main__.main surfaces as an orphan and is scoped out of the product denominator; the catalog honestly reports incomplete public-surface tag coverage — advisory, local-only, never gates

## [PASS] plainweave requirements-enrichment

over the covered+uncovered seed, plainweave requirements-enrichment reports cli._add_book present (bound, non-empty requirements), tour.__main__.main absent (recorded, unbound), and an unresolvable locator unavailable (identity gap — never a silent 'absent') — the Warpline-facing no-silent-clean contract; advisory, local-only, never gates

## [PASS] plainweave wardline peer facts

plainweave reads .wardline/ snapshots as advisory peer facts: an active defect and a non-defect finding surface; a finding gone from the latest in-scope snapshot is reported resolved_or_unseen while an out-of-scope prior finding is honestly flagged (scope mismatch), not silently resolved; and an absent .wardline/ is unavailable, never clean — advisory, local-only, never gates

## [PASS] plainweave coverage

seeded the covered+uncovered corpus, then exercised plainweave's baseline, verification, and dossier surfaces: locking a baseline and superseding one approved requirement makes baseline diff report it as drift while an untouched requirement stays unchanged, and diffing a baseline in a store with no locked baseline reports an honest error rather than a silent clean; a requirement with a method and passing evidence reports satisfied while one with a method but no evidence reports unverified (never silently satisfied) and orphaned evidence reports stale; and the requirement dossier is coherent for a known requirement and reports an honest error for an unknown one — never an empty-as-clean dossier; advisory, local-only, never gates

## [PASS] mcp attachment

all .mcp.json members reachable MCP-first and bound to the staged repo — federation seam integrity asserted; a silent de-attach trips this gate

## [PASS] wardline scan

surfaced rules: PY-WL-101, PY-WL-102, PY-WL-103, PY-WL-104, PY-WL-105, PY-WL-106, PY-WL-107, PY-WL-108, PY-WL-109, PY-WL-110, PY-WL-111, PY-WL-112, PY-WL-113, PY-WL-114, PY-WL-115, PY-WL-116, PY-WL-117, PY-WL-118, PY-WL-119, PY-WL-120, PY-WL-121, PY-WL-122, PY-WL-123, PY-WL-124, PY-WL-125, PY-WL-126, WLN-ENGINE-FLOW-INSENSITIVE-FALLBACK, WLN-ENGINE-METRICS, WLN-ENGINE-UNKNOWN-IMPORT, WLN-L3-LOW-RESOLUTION

## [PASS] wardline fail-closed gate

unparseable specimen trips the gate: WLN-ENGINE-PARSE-ERROR, exit 1 (fail-closed, not silent)

## [PASS] wardline scan (rust)

surfaced rules: RS-WL-108, RS-WL-112, WLN-RUST-COVERAGE

## [PASS] legis govern

governed 43 active defects → surface_override

## [PASS] legis policy-boundary-check

boundary-evidence check discriminates: disabled-evidence boundary flagged, healthy boundary passes

## [PASS] legis reject malformed artifact

absent findings key REJECTED (HTTP 422) — fail-closed, never zero-under-green

## [PASS] legis posture floor

signed posture floor is provisioned and readable: an operator-minted GENESIS pins the minimum enforcement cell in an append-only, hash-chained ledger; reads need no operator key, raising the floor needs `legis posture set` under an open operator session — so posture_get / policy_list answer over MCP instead of failing closed

## [PASS] filigree list

live — Wardline findings POST to the filigree scan-results bridge

## [PASS] filigree work cycle

sentinel issue cycled: promote (idempotent) → claim by actor → close; priority-range filter and suppressed-severity rollup asserted
